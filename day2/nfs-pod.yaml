apiVersion: v1
kind: Pod
metadata:
  name: nfs-pod
spec:
  containers:
  - image: nginx 
    name: web
    volumeMounts:
    - mountPath: /usr/share/nginx/html
      name: index-vol
  volumes:
  - name: index-vol
    nfs:
      server: 192.168.189.128
      path: /var/nfs-share
